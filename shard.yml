name: marten
version: 0.1.0.dev0

authors:
  - Morgan Aubert <me@morganaubert.name>

crystal: 0.32.1

dependencies:
  db:
    github: crystal-lang/crystal-db

development_dependencies:
  ameba:
    github: crystal-ameba/ameba

targets:
  marten-admin:
    main: src/admin_cli.cr

scripts:
  postinstall: scripts/precompile_admin_cli

license: MIT
